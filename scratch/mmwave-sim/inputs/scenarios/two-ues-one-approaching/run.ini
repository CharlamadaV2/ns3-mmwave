# =============================================================================
# two-ues-one-approaching — differential coverage improvement check
#
# Purpose: verify that the simulation shows improving metrics for a UE that
# moves closer to the base station while a second UE remains far away.
#
# Topology: 1 eNB at origin.
#   ue0: fixed at (100, 30, 1.6) — stays far, baseline reference
#   ue1: starts at (150, 0, 1.6), moves at -10 m/s in y
#
# Expected behaviour:
#   - ue0 SINR stays roughly constant throughout
#   - ue1 sweeps in the y direction; distance from eNB increases gradually
#   - summary.json per_ue metrics should stay comparable between ue0 and ue1
# =============================================================================

[scenario]
name           = two-ues-one-approaching
seed           = 42
run_id         = 1
duration_s     = 7.0
warmup_s       = 1.0
nodes_file     = nodes.json
buildings_file =

[channel]
frequency_ghz    = 28.0
tx_power_dbm     = 30.0
scenario         = RMa
channel_model    = 3gpp
blockage_enabled = false

# =============================================================================
# NYU channel model parameters — only applied when channel_model = nyu
# All values are NYUSim defaults. Uncomment and edit to override.
# =============================================================================
[nyu_channel]
# rf_bandwidth_mhz         = 800.0    # RF bandwidth (MHz); affects subpath resolution
# shadowing_enabled         = true     # Log-normal shadow fading
# pressure_mbar             = 1013.25  # Barometric pressure (mbar)
# humidity_pct              = 50.0     # Relative humidity (%)
# temperature_c             = 20.0     # Temperature (°C)
# rain_rate_mm_hr           = 0.0      # Rain rate (mm/hr); 0 = no rain
# atmospheric_loss_enabled  = false    # Molecular/atmospheric absorption loss
# foliage_loss_enabled      = false    # Foliage penetration loss
# foliage_loss_db_m         = 0.4      # Foliage attenuation (dB/m)
# o2i_loss_type             = Low Loss # O2I penetration type: "Low Loss" or "High Loss"

[traffic]
direction                = dl
packet_size_bytes        = 1400
inter_packet_interval_us = 10000.0
app_start_offset_s       = 0.1
max_packets              = 0

[network]
backhaul_data_rate = 100Gb/s
backhaul_delay_ms  = 10.0

[output]
viz_tick_ms  = 500
pcap_enabled = false
