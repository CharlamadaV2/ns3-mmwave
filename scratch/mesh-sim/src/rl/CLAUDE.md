# rl/

## Scope
RL agent placeholder. Returns current positions unchanged (no-op).
Will be replaced with an actual RL agent (Python IPC via ZMQ, stdin/stdout, or shared memory).

## Files
- **rl-agent.h** -- `RlAgent` class with `GetActions()` method. Header-only placeholder.

## Dependencies
- Depends on: `domain/`
- Depended on by: `sim.cc`
