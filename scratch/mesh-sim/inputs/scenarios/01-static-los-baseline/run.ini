# =============================================================================
# 01-static-los-baseline — link budget calibration at a known distance
#
# Purpose: simplest possible scenario — two fixed nodes, no mobility, no
# buildings. Every output graph should be a flat line. If anything isn't
# flat, the simulator has a time-dependent bug.
#
#   node-a:  (0, 0, 10)   — fixed
#   node-b:  (100, 0, 10) — fixed
#
# Link budget (approximate):
#   UMi LOS PL at 100m, 28 GHz ≈ 104 dB
#   Rx power ≈ 30 - 104 + 24 = -50 dBm
#   SINR ≈ -50 - (-83) = 33 dB
#   Shannon capacity ≈ 400 * log2(1 + 10^3.3) ≈ 4400 Mbps
#
# Expected behaviour:
#   - All time-series plots are flat horizontal lines
#   - SINR ~30-35 dB, capacity ~4000-4500 Mbps
#   - MCS stuck at CQI 14 (highest)
#   - Flow throughput = 10 Mbps (demand fully met)
#   - Latency ≈ 0.5 ms (0.5 ms processing + negligible propagation)
# =============================================================================

[scenario]
name           = 01-static-los-baseline
seed           = 1
run_id         = 1
duration_s     = 5.0
warmup_s       = 0.0
tick_s         = 0.1
nodes_file     = nodes.json
buildings_file =

[channel]
frequency_ghz    = 28.0
tx_power_dbm     = 30.0
scenario         = UMi
channel_model    = 3gpp
blockage_enabled = false
bandwidth_mhz   = 400.0
noise_figure_db  = 5.0

[traffic]
model          = constant
demand_mbps    = 10.0
flow_topology  = all_pairs

[routing]
algorithm = shortest_path
max_hops  = 5

[output]
viz_tick_ms = 100
